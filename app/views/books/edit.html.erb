<h1>図書の返却</h1>

<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%#= debug @syoko %>
  <% list = [] %>
  <% @syoko.each_with_object([]) do |s, _| %>
    <% list.push([s.place, s.id]) %>
    <%#= debug _ %>
  <% end %>
  <div class="field">
    返却先<br>
    <%#= f.select :user_id, [["17Fベイ側書庫", "1"], ["17Fリバー側書庫", "2"]], :class => "dropdown-menu" %>
    <%= f.select :user_id, list, :class => "dropdown-menu" %><br>
  </div>
  <div class="actions">
    <%= f.submit '返却', :class => 'btn btn-primary' %>
  </div>
<% end %>

<%= link_to 'Show', @book %> |
<%= link_to 'Back', books_path %>
